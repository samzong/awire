cli:
  name: awirectl
  short: awirectl management CLI

auth:
  validate:
    method: GET
    path: /api/v1/whoami
    display:
      username_field: service
      fallback_field: service

update:
  github:
    owner: samzong
    repo: awire
    asset: "awirectl_{{ .Version }}_{{ .OS }}_{{ .Arch }}.tar.gz"
